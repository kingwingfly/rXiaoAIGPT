[package]
name = "music_backend"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.44.1", features = [
    "macros",
    "rt-multi-thread",
    "signal",
] }
axum = { version = "0.8.1" }
tower = { version = "0.5.2" }
tower-http = { version = "0.6.2", features = ["fs"] }
